
  <ul class="nav navbar-nav navbar-right">
    <li class="dropdown">
      <% if current_user %>
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            <span class="glyphicon glyphicon-user"></span> <%= render :text => current_user %> <span class="caret"></span>
          </a>
          <ul class="dropdown-menu" role="menu">
            <li><a href="<%= destroy_user_session_path%>"><%= t('.logout') %></a></li>
          </ul>
      <% else %>
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            <span class="glyphicon glyphicon-user"></span> <%= t('.guest') %> <span class="caret"></span>
          </a>
          <ul class="dropdown-menu" role="menu">
            <li><a href="<%= new_user_session_path%>"><%= t('.login') %></a></li>
          </ul>
      <% end %>
    </li>
  </ul>
