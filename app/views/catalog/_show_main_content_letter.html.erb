<%# default partial to display solr document fields in catalog show view -%>
<% id = @document.id %>
<% coll = "/db#{ id[0,id.rindex('/')] }" %>


      <div class="nav-tab-instance">
        <ul class="nav nav-tabs">
          <li  class="active">
              <%= link_to(solr_document_path(@document.id), toggle:"tab") do %>
                  Tekst
              <% end %>
          </li>

          <li>
            <% book_id, div_id = @document.id.split('-') %>
            <%= link_to(solr_document_path(book_id), toggle:"tab" ) do %>
              Letter
            <% end %>
          </li>
          <li role="presentation">
            <%# Note we need to use no_turbolink because turbolinks will fuck with the unveil library %>
            <%= link_to(facsimile_catalog_path(@document.id) + "/", data: { no_turbolink: true }) do %>
                Faksimile
            <% end %>
          </li>
        </ul>
      </div>

      <div >
        <div id="<%= solr_document_path(@document.id) %>" class="tab-pane text active">
          <!--
          < % text ||= SnippetServer.render_snippet(@document.id,{:c=> coll}) %>
          < %= text.html_safe %>
          -->
          test 1
        </div>
        <div id="<%= solr_document_path(book_id)%>" class="tab-pane text">
          <!--
          < % book_id, div_id = @document.id.split('-') %>
          < % text ||= SnippetServer.render_snippet(book_id) %>
          < %= text.html_safe %>
          -->
          Test
        </div>
      </div>
