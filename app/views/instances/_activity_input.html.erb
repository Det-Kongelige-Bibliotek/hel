<%=
    if @instance.activity.blank? || current_user.admin?
        builder.input :activity, collection: Administration::Activity.all, prompt: t('.activity'), label_method: :activity,
                      input_html: { data: { function: 'fetch_activity_data' }}
    else
      "Aktivitet: #{@instance.activity}"
    end
%>