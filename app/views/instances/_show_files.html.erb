<a id="upload"></a><h4><%= t('.label')%></h4>
<% @instance.content_files.each do |f| %>
    <strong><%= f.original_filename %></strong>
    <table class="table table-striped"><tbody>
    <tr>
      <td> <strong><%= t('.file_name')%></strong></td>
      <td>
        <%= f.original_filename %>
      </td>
    </tr>
    <tr>
      <td>
        <strong><%= t('.bev_profil')%></strong>
      </td>
      <td>
        <span><%= @instance.preservation_profile %></span>
      </td>
    </tr>
    <tr>
      <td><strong><%= t('.bev_status')%></strong></td>
      <td><%= f.preservation_state %></td>
    </tr>
    <tr>
      <td><strong><%= t('.bev_detaljer')%></strong></td>
      <td><%= f.preservation_details %></td>
    </tr>
    <tr>
      <td><strong><%= t('.download')%></strong></td>
      <td>
        <%if can?(:download,@instance.content_files.first) %>
            <%= link_to "Download", download_content_file_path(f)%>
        <% end %>
      </td>
    </tr>
    <% if f.content_can_be_changed? %>
        <tr>
          <td><strong><%= t('.upload')%></strong></td>
          <td>
            <%if can?(:download,@instance.content_files.first) %>
                <%= link_to "Upload", '#upload_file_modal', {"data-toggle" => "modal", "data-target" => "#upload_file_modal"} %>
            <% end %>
          </td>
        </tr>
    <% end %>
</tbody></table>
<%= render partial: 'instances/upload_file_modal', :locals => { :file => f }%>

<% end %>
