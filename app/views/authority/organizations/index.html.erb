<h1><%= t('.list', var: @klazz.to_s) %></h1>

<table class="table">
  <thead>
  <tr>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @authority_bases.each do |org  | %>
      <tr>
        <td><%= link_to org.display_value, org %></td>
        <td><%= link_to 'Edit', edit_authority_organization_path(org) %></td>
        <td><%= link_to 'Destroy', authority_organization_path(org), method: :delete, data: { confirm: t('.confirm') } %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Base', new_authority_organization_path %>