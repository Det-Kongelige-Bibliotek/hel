<% remote ||= false %>
<% @authority_object ||= Authority::Person.new %>
<%= simple_form_for(@authority_object, remote: remote, html: { class: 'col-sm-6' },
                    defaults: { input_html: { class: 'form-control' }}  ) do |f| %>
    <%= f.input :given_name %>
    <%= f.input :family_name %>
    <%= f.input :birth_date %>
    <%= f.input :death_date %>
    <%= f.input :description %>
    <%= f.input :same_as, as: :url %>
  <div class="actions">
    <%= f.submit 'Gem Person', class: 'btn btn-lg btn-success' %>
  </div>
<% end %>
