<h1><%= t('.list', var: @klazz.to_s) %></h1>

<table class="table">
  <thead>
  <tr>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @authority_bases.each do |person| %>
      <tr>
        <td><%= link_to person.display_value, person %></td>
        <td><%= link_to 'Edit', edit_authority_person_path(person) %></td>
        <td><%= link_to 'Destroy', authority_person_path(person), method: :delete, data: { confirm: t('.confirm') } %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Base', new_authority_person_path %>